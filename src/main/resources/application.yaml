spring:
  application:
    name: chemflow
  datasource:
    url: jdbc:postgresql://localhost:5432/chemflow
    username: postgres
    password: Shouvik10!
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true